services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped

  console:
    build:
      context: ./console
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    # 不 scale，維持單一實例執行排程任務
